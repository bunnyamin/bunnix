# `-rw------- ~/.ssh/config`

Host <NAME, ALIAS 1>
  HostName        <DOMAIN, IP>
  Port            <PORT>
  IdentityFile    <PATH TO PRIVATE KEY>~/.ssh/key1.pub
  IdentitiesOnly  yes

Host <name, alias 2>
  HostName        <DOMAIN, IP>
  Port            <PORT>
  IdentityFile    <PATH TO PRIVATE KEY>~/.ssh/key2.pub
  IdentitiesOnly  yes

